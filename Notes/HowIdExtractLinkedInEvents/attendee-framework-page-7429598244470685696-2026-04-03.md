# LinkedIn Attendee Framework — Page 7429598244470685696

Date: 2026-04-03
Page: https://www.linkedin.com/events/7429598244470685696/?viewAsMember=true
Managed tab: 1729153367

## Goal for this pass

Turn slop-browser's LinkedIn work into a framework surface, not a one-off event scraper.

This page is the first concrete target for the next LinkedIn capability:
- open event page
- click Manage
- click Manage attendees
- increase attendee request size from 20 to 50 by rewriting request params
- page through results no faster than 1 batch/sec
- extract attendee row data
- enrich attendee profiles with deeper LinkedIn profile details
- return structured CLI-friendly data

## Live findings on this page

### Event page basics confirmed
- Title on page: `AI Hackers Are Here: Why RSAC and Community Unity Matter More Than Ever`
- Organizer link present: `Event by Hacker Valley Media`
- Event attendee summary visible: `Yuvraj Singh and 65 other attendees`

### Manage flow confirmed
From live `state/tree` inspection:
- `Manage` button exists on page
- In one successful open state, the dropdown exposed:
  - `Manage attendees`
  - `Delete event`

### Manage attendees modal confirmed
In one successful live modal capture, I observed:
- dialog title: `Manage attendees`
- tabs:
  - `Attendees`
  - `Invited`
- total shown in modal: `66 people`
- visible CTA at bottom: `Show more results`
- close CTA: `Done`

### Attendee row data already visible in the modal
The modal rows already expose strong row-level data without leaving the modal.
Examples captured live:
- `ONUR KARAN` — headline includes:
  - `Cyber Security Professional | CEH | ECIH | CSA | CHFI | ISO 27001 L/A | Palo Alto NGFW Engineer | SOC Analyst | Incident Handler | Cyber Security Instructor | System Engineer`
- `Senthil Ramakrishnan` — headline:
  - `Cybersecurity | Connectivity | 5G`
- `Linton Harris CISSP` — headline:
  - `Experienced Cyber Security Leader | Defender Against Cyber Threats | Strategic Risk Management | Incident Response Commander |`
- `Dewayne Hart` — headline with long role summary visible in modal row
- `Saurabh Dubey` — headline visible in modal row
- `Hubbert Smith` — headline visible in modal row

This confirms the attendee modal row extractor should at minimum capture:
- display name
- connection degree
- headline
- profile URL slug
- visible row controls / affordances

## API pattern grounding

I was not able to successfully pull the exact attendee request from live network logs during the unstable WS window, but I do have strong LinkedIn-specific evidence from the reference repo via `gh`.

Reference repo: `ronaldeddings/linkedin-extension`

Useful file:
- `src/utils/Events/GetEventAttendeesByID.ts`

Known attendee endpoint pattern from that repo:
- `https://www.linkedin.com/voyager/api/graphql?...queryId=voyagerSearchDashClusters...`
- query variables include:
  - `count`
  - `start`
  - `origin: EVENT_PAGE_CANNED_SEARCH`
  - query param list containing:
    - `eventAttending`
    - `resultType: PEOPLE`

That is the right override target for increasing the attendee batch size.

## Framework implications

This should not live as generic DOM scraping. We need LinkedIn framework modules organized by capability.

### Capability areas now clearly emerging
1. **LinkedIn event page extraction**
2. **LinkedIn attendee modal extraction**
3. **LinkedIn attendee search request rewriting**
4. **LinkedIn attendee profile enrichment**
5. **LinkedIn post/social detail extraction**
6. **LinkedIn company / employer enrichment**

## Concrete extraction targets for attendee framework

### Modal row extraction
For each attendee row, extract:
- full display name
- first name
- last name
- connection degree if shown
- LinkedIn headline
- profile URL
- any row badge/status metadata

### Profile enrichment extraction
For each attendee profile, extract when available:
- full name
- first name
- last name
- headline
- about
- follower count
- location
- current title
- current company
- company location / employer HQ if surfaced through page/API enrichment
- current roles
- previous roles
- recent post / repost signal if visible

## Important live behavior constraints

- The modal uses batched loading via `Show more results`
- We should not advance faster than **1 batch per second**
- Request rewriting should prefer increasing `count` so fewer UI batch actions are needed
- The CLI result must stay structured and agent-friendly

## Immediate next implementation targets

1. Add a dedicated LinkedIn attendee flow command surface in slop-browser.
2. Add LinkedIn-specific request override preset for attendee search:
   - rewrite attendee search `count` from default to `50`
3. Add modal row extraction module.
4. Add paced batch loader (`>= 1 second` between batch fetches).
5. Add profile enrichment module and return shape.
