#pragma once
/* Public header for the C loader target. The constructor in loader.c needs no
 * exported API; this header exists only because SwiftPM requires an include/
 * directory for a C target. */
