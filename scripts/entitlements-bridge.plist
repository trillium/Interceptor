<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Required so interceptor-bridge can send Apple Events to other apps
         under hardened runtime. macOS still prompts the user on first dispatch
         per (interceptor-bridge, target_app) pair via TCC. -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <!-- Microphone capture entitlement. Per Apple's "Requesting Authorization
         for Media Capture on macOS": NSMicrophoneUsageDescription in Info.plist
         AND com.apple.security.device.audio-input as an entitlement are BOTH
         required under hardened runtime; without the entitlement the system
         silently terminates the requestAccess call rather than showing the
         permission alert. Source:
         /BundleResources/requesting-authorization-for-media-capture-on-macos.md
         /AVFoundation/requesting-authorization-to-capture-and-save-media.md -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- personal-data domains. tccd silently denies privacy prompts
         under hardened runtime when the matching entitlement is absent —
         confirmed in unified log: "Prompting policy for hardened runtime;
         service: kTCCServiceCalendar requires entitlement
         com.apple.security.personal-information.calendars but it is missing".
         Same gate hits Reminders, Contacts, Photos, and Location. -->
    <key>com.apple.security.personal-information.calendars</key>
    <true/>
    <key>com.apple.security.personal-information.addressbook</key>
    <true/>
    <key>com.apple.security.personal-information.photos-library</key>
    <true/>
    <key>com.apple.security.personal-information.location</key>
    <true/>
    <!-- Bridge-only Virtualization framework requirement. Enables
         VZVirtualMachine + VZVirtualMachineConfiguration + VZMacOSRestoreImage
         (Virtualization.md:18). Apple does not restrict this entitlement —
         any Developer ID-signed app may carry it
         (BundleResources/Entitlements/com.apple.security.virtualization.md).
         ships VM lifecycle with VZNATNetworkDeviceAttachment, which
         requires only this entitlement. Bridged + vmnet attachment modes
         (com.apple.vm.networking) are out of scope per 's scope
         decision header — Apple restricts that entitlement via a private
         allowlist Interceptor is not pursuing. -->
    <key>com.apple.security.virtualization</key>
    <true/>
</dict>
</plist>
